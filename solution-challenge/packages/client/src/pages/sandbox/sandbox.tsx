import React from 'react';

type SandboxProps = {
    language: string
}

const Sandbox: React.FC<SandboxProps> = ({ language }) => {
    return(
        <div>
            
        </div>
    )
}

export default Sandbox;